# Отладка Flutter приложения на Android устройстве по USB

## 1. Подготовка к отладке

### Шаг 1: Установить Android Debug Bridge (ADB)
- **Вариант A**: Установите Android Studio (включает ADB)
- **Вариант B**: Установите только SDK Platform Tools:
  - Скачайте с https://developer.android.com/studio/releases/platform-tools
  - Распакуйте в удобное место (например, C:\Android\platform-tools)

### Шаг 2: Добавить ADB в переменные окружения (Windows)
```
1. Нажмите Win + X → Система
2. Расширенные параметры системы → Переменные окружения
3. Нажмите "Изменить..." для Path
4. Добавьте путь: C:\Android\platform-tools (или ваш путь)
5. ОК → ОК
```

Проверьте установку в PowerShell:
```powershell
adb version
```

---

## 2. Включение режима разработчика на Android

### На устройстве:
1. Откройте **Параметры**
2. Перейдите в **О телефоне** (или О планшете)
3. Нажмите 7 раз на **Номер сборки** (Build Number)
4. Появится сообщение "Вы стали разработчиком"
5. Вернитесь в **Параметры** → **Параметры разработчика**
6. Включите:
   - ✅ **Отладка по USB** (USB Debugging)
   - ✅ **Установка из неизвестных источников** (если требуется)
   - ✅ **Выбрать тип USB-соединения: Передача файлов/MTP** или **Только зарядка** (затем выберите PTP или MTP)

---

## 3. Подключение устройства к компьютеру

### Процесс подключения:
1. Подключите Android устройство к компьютеру через USB кабель
2. На устройстве может появиться запрос "Разрешить отладку по USB" → **Да/Allow**
3. Отметьте "Всегда разрешать с этого компьютера" (если требуется)

### Проверка подключения в PowerShell:
```powershell
adb devices
```

Вывод должен быть похож на:
```
List of attached devices
a1b2c3d4e5f6g7h8 device
```

Если написано `unauthorized`, повторите разрешение на устройстве.

---

## 4. Запуск приложения с отладкой

### Способ 1: В VS Code
```powershell
# В терминале VS Code (или PowerShell в папке проекта)
flutter run
```

Приложение автоматически запустится на подключённом устройстве.

### Способ 2: С дополнительными флагами
```powershell
# С логами в консоль
flutter run -v

# С горячей перезагрузкой
flutter run  # затем нажимайте 'r' для hot reload, 'R' для hot restart
```

### Способ 3: Запуск в режиме Release
```powershell
flutter run --release
```

---

## 5. Просмотр логов приложения

### В реальном времени:
```powershell
flutter logs
```

### С фильтром конкретного приложения:
```powershell
adb logcat | findstr "my_app"
```

### Все логи:
```powershell
adb logcat
```

### Очистить логи:
```powershell
adb logcat -c
```

---

## 6. Тестирование уведомлений на реальном устройстве

После подключения и `flutter run`:

1. **Используйте виджет для тестирования** из `NotificationTestWidget`
2. Нажмите кнопку "Показать уведомление"
3. Уведомление должно появиться в системном трее

### Если уведомления не видны:

**Проверка 1**: Разрешения приложению
```powershell
# На устройстве: Параметры → Приложения → my_app → Разрешения
# Включите: Уведомления (Notifications)
```

**Проверка 2**: Просмотрите логи ошибок
```powershell
flutter logs
```

**Проверка 3**: Убедитесь, что канал (channel) создан правильно
- В `notification_service.dart` проверьте канал 'phishing_alerts'

---

## 7. Отладка приложения (Debugging)

### Установка брейкпойнтов в VS Code:
1. Откройте файл (например, `auth_service.dart`)
2. Кликните на номер строки слева (появится красная точка)
3. Нажмите `F5` или запустите через Run → Start Debugging
4. Когда код достигнет точки, приложение приостановится
5. Используйте:
   - **F5** - продолжить (continue)
   - **F10** - следующая строка (step over)
   - **F11** - шаг внутрь (step into)
   - **Shift+F11** - шаг выхода (step out)

### Просмотр переменных:
- В левой панели "Debug" видны все локальные переменные
- Наведите на переменную в коде для подсказки

---

## 8. Полезные команды ADB

```powershell
# Список всех подключённых устройств
adb devices -l

# Информация об устройстве
adb shell getprop ro.build.version.release    # Android версия
adb shell getprop ro.product.model            # Модель

# Переустановить приложение
adb install -r app-debug.apk

# Удалить приложение
adb uninstall com.example.my_app

# Открыть приложение
adb shell am start -n com.example.my_app/.MainActivity

# Остановить приложение
adb shell am force-stop com.example.my_app

# Скопировать файл с устройства на ПК
adb pull /sdcard/DCIM/photo.jpg C:\Users\Admin\Downloads\

# Скопировать файл с ПК на устройство
adb push file.txt /sdcard/

# Перезагрузить устройство
adb reboot
```

---

## 9. Решение проблем

| Проблема | Решение |
|----------|---------|
| `adb: command not found` | Добавьте путь к ADB в переменные окружения |
| `no devices found` | Включите режим разработчика, проверьте кабель, переподключитесь |
| `unauthorized` | Разрешите отладку на самом устройстве |
| Приложение не устанавливается | `adb install -r app-debug.apk` или удалите старую версию |
| Уведомления не появляются | Проверьте разрешения приложения в параметрах устройства |
| Горячая перезагрузка не работает | Перезагрузитесь: `adb shell am am force-stop com.example.my_app` |

---

## 10. Быстрая шпаргалка для начала отладки

```powershell
# 1. Проверить подключение устройства
adb devices

# 2. Включить режим разработчика на устройстве (нажать 7 раз на Build Number)

# 3. Запустить приложение
flutter run

# 4. Просмотреть логи
flutter logs

# 5. Установить точку останова в VS Code и нажать F5
```

---

Теперь вы готовы отлаживать приложение на реальном устройстве! 🎉
