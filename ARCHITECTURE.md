# Структура проекта VEIL

## 📁 Организация файлов

```
lib/
├── main.dart                      # Точка входа приложения, AuthWrapper
├── screens/
│   ├── login_screen.dart          # Экран входа (обертка)
│   ├── register_screen.dart       # Экран регистрации (обертка)
│   ├── home_screen.dart           # Главный экран (обертка)
│   ├── details_screen.dart        # Экран деталей (обертка)
│   └── profile_screen.dart        # Экран профиля (обертка)
│
├── widgets/
│   ├── login_widgets/
│   │   └── login_screen_section.dart      # Логика входа
│   ├── register_widgets/
│   │   └── register_screen_section.dart   # Логика регистрации
│   ├── home_widgets/
│   │   ├── home_screen_section.dart
│   │   ├── alert_card.dart
│   │   ├── info_chip.dart
│   │   ├── phishing_alert_banner.dart
│   │   ├── protection_status_banner.dart
│   │   ├── quick_action_buttons.dart
│   │   ├── recent_alerts.dart
│   │   └── tip_of_the_day_banner.dart
│   ├── details_widgets/
│   │   ├── details_screen_section.dart
│   │   ├── alert_details_card.dart
│   │   └── recommendation_item.dart
│   ├── profile_widgets/
│   │   └── profile_screen_section.dart
│   ├── alert_card.dart
│   ├── info_chip.dart
│   ├── phishing_alert_banner.dart
│   ├── protection_status_banner.dart
│   ├── quick_action_buttons.dart
│   ├── recent_alerts.dart
│   └── tip_of_the_day_banner.dart
│
├── services/
│   ├── auth_service.dart          # Аутентификация и SharedPreferences
│   └── phishstats_api.dart        # API сервис
│
└── data/
    └── security_tips.dart         # Статические данные
```

## 🔐 Система аутентификации

### AuthService (`services/auth_service.dart`)
- **register(username, password)** - Регистрация нового пользователя
- **login(username, password)** - Вход в систему
- **logout()** - Выход из системы
- **getCurrentUser()** - Получить текущего пользователя
- **isLoggedIn()** - Проверить статус авторизации

### Особенности
- Пароли хэшируются SHA256 перед сохранением
- Пароли хранятся только в хэшированном виде
- Логины хранятся в открытом виде
- Данные сохраняются в SharedPreferences локально
- Проверка на существующие логины при регистрации

### Экраны аутентификации
- **LoginScreen** - Вход с переходом на регистрацию
- **RegisterScreen** - Регистрация с валидацией паролей

### Экран профиля
- **ProfileScreen** - Показывает данные текущего пользователя
- Возможность выхода из аккаунта

## 🎯 Поток приложения

1. **AuthWrapper** проверяет статус авторизации при запуске
2. Если не авторизован - показывает LoginScreen/RegisterScreen
3. Если авторизован - показывает MainNavigation (основное приложение)
4. MainNavigation содержит 3 вкладки: Home, Details, Profile
5. ProfileScreen позволяет выйти из аккаунта

## 📦 Зависимости

```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.2.0
  crypto: ^3.0.3
```

## ✅ Требования задания (выполнено)

- ✅ Экран регистрации с полями логина и пароля
- ✅ Экран входа с полями логина и пароля
- ✅ Использование SharedPreferences для локального хранения
- ✅ Логины хранятся в открытом виде
- ✅ Пароли хранятся в хэшированном виде (SHA256)
- ✅ Проверка существования логина при регистрации
- ✅ Сообщения об ошибках при неверных данных
- ✅ Переход на основной экран при успешном входе
- ✅ Возможность выхода из профиля
- ✅ Разделение экранов и виджетов по папкам
