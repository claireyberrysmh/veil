# Интеграция локальных уведомлений в VEIL

## 📋 Выполненные изменения

### 1. ✅ Добавлены зависимости в pubspec.yaml
- `flutter_local_notifications: ^16.3.0` - основной пакет для уведомлений
- `timezone: ^0.9.0` - для работы с плановыми уведомлениями

### 2. ✅ Создан сервис уведомлений
**Файл**: `lib/services/notification_service.dart`

Функционал:
- Инициализация системы уведомлений с поддержкой Android и iOS
- Показание простых уведомлений: `showNotification()`
- Показание плановых уведомлений: `showScheduledNotification()`
- Отмена уведомлений: `cancelNotification()` и `cancelAllNotifications()`
- Обработка кликов на уведомления

### 3. ✅ Обновлён main.dart
- Инициализация `NotificationService()` при запуске приложения
- Добавлено `WidgetsFlutterBinding.ensureInitialized()` для корректной работы

### 4. ✅ Настроена Android конфигурация
**Файл**: `android/app/src/main/AndroidManifest.xml`
- Добавлено разрешение: `POST_NOTIFICATIONS` (для Android 13+)

### 5. ✅ Создан виджет для тестирования
**Файл**: `lib/widgets/notification_test_widget.dart`

Содержит кнопки:
- "Показать уведомление" - мгновенное уведомление
- "Плановое уведомление (5 сек)" - уведомление с задержкой
- "Отменить все уведомления" - удаление всех уведомлений

### 6. ✅ Интегрирован виджет в главный экран
**Файл**: `lib/widgets/home_widgets/home_screen_section.dart`
- Добавлен `NotificationTestWidget()` на главный экран для удобного тестирования

### 7. ✅ Создано подробное руководство по отладке
**Файл**: `DEBUGGING_GUIDE.md`
- Пошаговые инструкции по подключению Android устройства
- Команды для отладки приложения
- Решение распространённых проблем

---

## 🚀 Следующие шаги

### Установка зависимостей:
```bash
flutter pub get
```

### Подключение Android устройства через USB:

1. **На компьютере (Windows)**:
   ```powershell
   # Установить Android Debug Bridge (ADB)
   # Или добавить платформу-tools в переменные окружения
   
   # Проверить подключение
   adb devices
   ```

2. **На устройстве**:
   - Включить режим разработчика (нажать 7 раз на "Номер сборки")
   - Включить "Отладку по USB"
   - Разрешить отладку с этого компьютера

3. **Запустить приложение**:
   ```powershell
   flutter run
   ```

4. **Тестировать уведомления**:
   - На главном экране появятся кнопки для тестирования
   - Нажмите "Показать уведомление"
   - Уведомление должно появиться в системном трее устройства

---

## 📱 Использование NotificationService в коде

### Показать простое уведомление:
```dart
import 'services/notification_service.dart';

NotificationService().showNotification(
  id: 1,
  title: 'Заголовок',
  body: 'Тело уведомления',
  payload: 'any_data',
);
```

### Показать уведомление через N секунд:
```dart
NotificationService().showScheduledNotification(
  id: 2,
  title: 'Плановое уведомление',
  body: 'Появится через 10 секунд',
  delay: const Duration(seconds: 10),
);
```

### Отменить уведомление:
```dart
NotificationService().cancelNotification(1);
```

---

## 🔧 Примеры интеграции в приложение

### Отправить уведомление о фишинг-угрозе:
```dart
import 'services/phishstats_api.dart';
import 'services/notification_service.dart';

// В методе проверки ссылки
Future<void> checkLink(String url) async {
  final post = await PhishstatsAPI.fetchPostByUrl(url);
  
  if (post != null && post.isPhishingThreat) {
    NotificationService().showNotification(
      id: DateTime.now().millisecondsSinceEpoch.hashCode,
      title: '⚠️ Обнаружена фишинг-ссылка',
      body: post.description,
      payload: post.id,
    );
  }
}
```

### Отправить уведомление при регистрации:
```dart
import 'services/auth_service.dart';
import 'services/notification_service.dart';

Future<void> registerUser(String username, String password) async {
  final success = await AuthService.register(username, password);
  
  if (success) {
    NotificationService().showNotification(
      id: 100,
      title: 'Добро пожаловать! 👋',
      body: 'Вы успешно зарегистрировались в VEIL',
    );
  }
}
```

---

## ⚠️ Важные примечания

1. **Android разрешения**: Приложение будет запрашивать разрешение на отправку уведомлений при первом запуске (для Android 13+)

2. **Тестирование на эмуляторе**: Если уведомления не видны на эмуляторе, попробуйте на реальном устройстве

3. **Canals (Каналы)**: Уведомления отправляются через канал 'phishing_alerts' - это стандартное поведение для Android 8+

4. **Payload**: Используйте `payload` для передачи данных, которые обрабатываются при нажатии пользователя на уведомление

---

## 📖 Полезные ссылки

- [flutter_local_notifications документация](https://pub.dev/packages/flutter_local_notifications)
- [Flutter Debugging Guide](DEBUGGING_GUIDE.md)
- [Android Developer Documentation](https://developer.android.com/)

---

**Готово к использованию!** 🎉
